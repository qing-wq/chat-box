<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ink.whi.chatboxbackend.dao.mapper.MessageMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="ink.whi.chatboxbackend.dao.entity.MessageDO">
        <id column="id" property="id" />
        <result column="conversation_id" property="conversationId" />
        <result column="parent_id" property="parentId" />
        <result column="content" property="content" />
        <result column="role" property="role" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, conversation_id, parent_id, content, role, create_time, update_time
    </sql>

    <!-- 基本的插入操作 -->
    <insert id="insert" parameterType="ink.whi.chatboxbackend.dao.entity.MessageDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message
        (conversation_id, parent_id, content, role, create_time, update_time)
        VALUES
        (#{conversationId}, #{parentId}, #{content}, #{role}, #{createTime}, #{updateTime})
    </insert>

</mapper>
